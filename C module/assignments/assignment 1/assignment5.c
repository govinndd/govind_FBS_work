#include<stdio.h>

void main(){
    int age = 10;
    if(age >= 18 && age <= 101){
        printf("the person is eligible to vote.");
    }
    else 
    printf("the person is not eligible to vote.");
}