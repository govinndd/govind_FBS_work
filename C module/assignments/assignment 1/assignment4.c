#include<stdio.h>
void main(){
    char ch = 'f';
    if(ch=='a' || ch=='e' || ch=='i' || ch=='o' || ch=='u' || ch=='A' || ch=='E' || ch=='I' || ch=='O' || ch=='U'){
        printf("the given char is a vowel.");

    }
    else
    printf("the given char is a consonant.");
}