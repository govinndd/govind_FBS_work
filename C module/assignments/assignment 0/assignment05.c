//Write a C program to input five numbers and find their average. 
#include <stdio.h>

void main()
{
    int no1 = 10, no2 = 20 , no3 = 30, no4 = 6, no5 = 9, average;
    average = no1 + no2 + no3 + no4 + no5;
    average = average / 5;
    printf("the average of 5 numbers are %d", average);

}