//Write a C program to find the square and cube of a given number.
#include<stdio.h>

void main ()
{
    int no = 3, square, cube;
    square = no * no;
    cube = no * no * no;
    printf("the no is %d \n the square of this number is %d \n the cube of this number is %d", no, square,cube);
}